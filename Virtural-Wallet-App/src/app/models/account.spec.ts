/*import { Account } from './account';
import { Payment } from './payment';

describe('Account', (accountNumber: string,
  balance: number,
  availableBalance: number,
 closed: number,
  payments: Payment[]) => {
  it('should create an instance', (accountNumber,balance,availableBalance,closed,payments)) => {
    expect(new Account(accountNumber,balance,availableBalance,closed,payments)).toBeTruthy();
  });
});*/
